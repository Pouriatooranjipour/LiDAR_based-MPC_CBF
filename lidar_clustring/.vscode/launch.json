{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Lidar Node",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/lidar_clustring/lidar_node.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/lidar_clustring:${PYTHONPATH}"
            }
        }
    ]
}